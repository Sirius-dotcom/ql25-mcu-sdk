Overwrite_int
==============

Function description:
---------------------
    This demo shows **RTC** in overwrite mode. Configure RTC as overflow interrupt. When the write 
    data exceeds the overflow upper limit, overflow interrupt will be generated.

Test result:
------------
   If output a overwrite interrupt,printf pass,if not printf fail.

IO connection:
--------------
NONE
