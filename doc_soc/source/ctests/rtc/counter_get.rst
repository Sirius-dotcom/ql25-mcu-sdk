Counter_get
============

Function description:
---------------------
    This demo shows counter get data.Configure second level interrupt and overflow interrupt, 
    print the value in **RTC_Counter** in each second interrupt, and generate overflow interrupt 
    in case of data overflow.

Test result:
------------
    When overflow interrupt is generated,printf pass,if not printf fail.

IO connection:
--------------
NONE