software_interrupt
==================

Function description:
---------------------
    * This demo shows LGPIO in interrupt mode.
    * Configure lgpio as external input interrupt.After the software interrupt is configured, it will be generated.
    
Test result:
------------
If software trigger a interrupt,printf pass,if not printf fail.

IO connection:
--------------
None
